package SRP.Ejemplo1;

public class CustomerDatabase
{
    public void saveCustomerToDatabase(Customer customer) {
        System.out.println("Customer '"+customer.getName()+"' saved into the database");
    }
}